{
  "name": "Validate the template has an issue attached",
  "action": "status",
  "eventJsonPath": [
    "$[?(!@.body.match(/Issue Reference.+:/i))].body",
    "$[?(@.body.match(/Issue Reference.+#[0-9]+/i))].body"
  ]
}
